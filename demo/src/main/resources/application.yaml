
spring:
  application:
    name: demoService

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
    fetch-registry: true
    register-with-eureka: true

server:
  port: 1111
